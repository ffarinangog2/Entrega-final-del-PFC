apiVersion: 1
providers:
  - name: SCLI
    folder: SCLI
    type: file
    options:
      path: /var/lib/grafana/dashboards
  - name: SCLI consolidado
    folder: SCLI
    type: file
    options:
      path: /var/lib/grafana/canonical
