import type {ReactNode} from 'react';import {render,screen,fireEvent} from '@testing-library/react';import {beforeEach,describe,expect,it,vi} from 'vitest';import {NotificationsPage} from './NotificationsPage';import * as api from '../../services/operationalApi';
vi.mock('../../components/DashboardLayout',()=>({DashboardLayout:({children}:{children:ReactNode})=><>{children}</>}));vi.mock('../../services/operationalApi',()=>({listarNotificaciones:vi.fn(),marcarNotificacionLeida:vi.fn(),marcarTodasNotificacionesLeidas:vi.fn()}));
describe('NotificationsPage',()=>{beforeEach(()=>{vi.mocked(api.listarNotificaciones).mockResolvedValue([{id:'n1',titulo:'Planificación aprobada',cuerpo:'Horario disponible',tipo:'PLAN',referenciaId:null,leida:false,creadaEn:'2026-09-05T10:00:00Z'}]);vi.mocked(api.marcarNotificacionLeida).mockResolvedValue({id:'n1',titulo:'Planificación aprobada',cuerpo:'Horario disponible',tipo:'PLAN',referenciaId:null,leida:true,creadaEn:'2026-09-05T10:00:00Z'});vi.mocked(api.marcarTodasNotificacionesLeidas).mockResolvedValue(undefined)});it('lista y permite marcar una notificación propia como leída',async()=>{render(<NotificationsPage/>);expect(await screen.findByText('Planificación aprobada')).toBeInTheDocument();fireEvent.click(screen.getByRole('button',{name:'Marcar como leída'}));expect(api.marcarNotificacionLeida).toHaveBeenCalledWith('n1')})})
